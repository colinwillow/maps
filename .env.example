# Copy to .env and fill in. Vite only exposes vars prefixed VITE_.

# MapTiler API key — maptiler.com, free tier. Without it the app boots on a
# minimal offline fallback style (dev/test only) and says so on screen.
VITE_MAPTILER_KEY=
